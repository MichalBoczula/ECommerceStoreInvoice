# Copy to .env for a local MongoDB without authentication; keep real credentials outside git.
# Bash: set -a; source .env; set +a; dotnet run --project src/ECommerceStoreInvoice.API
MongoDbSettings__ConnectionString=mongodb://localhost:27017
